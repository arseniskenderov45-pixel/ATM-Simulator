# JavaFX ATM Simulator 🏧

## Обзор проекта
Симулятор банкомата (ATM), разработанный на языке Java с использованием библиотеки JavaFX. Приложение имитирует реальный интерфейс банкомата, поддерживает регистрацию пользователей, финансовые транзакции и сохранение данных.

[Ссылка на видео объяснение](https://youtu.be/y51eGY5D3gY)


## Функциональные возможности
1.  **Авторизация и Регистрация:** Создание новых аккаунтов с проверкой уникальности и валидацией ПИН-кода.
2.  **Финансовые операции:** Пополнение, снятие наличных и перевод средств другим клиентам банка.
3.  **История транзакций:** Просмотр последних 10 операций для текущей сессии.
4.  **Персистентность данных:** Все аккаунты и балансы сохраняются в файл `users.txt`.

## Используемые технологии
* **Java 17+**
* **JavaFX** (GUI)
* **Java I/O** (File processing)

## Алгоритмы и Структуры данных

### Структуры данных
1.  **HashMap (`Map<String, UserAccount>`)**:
    * Используется для хранения базы данных пользователей.
    * *Почему:* Обеспечивает быстрый доступ (O(1)) к данным пользователя по его имени при входе в систему.
2.  **ArrayList (`List<String>`)**:
    * Используется для хранения истории транзакций.
    * *Почему:* Удобно добавлять новые записи и динамически изменять размер списка.

### Реализованные алгоритмы
1.  **Алгоритм аутентификации:** Проверка наличия ключа в HashMap и сопоставление хэша/строки пароля.
2.  **Линейная обработка файлов:** При запуске программа считывает файл построчно, парсит строку разделителем `;` и восстанавливает объекты.
3.  **Валидация ввода:** Использование регулярных выражений (`\\d+`) для проверки, что ПИН-код состоит только из цифр.

## Обработка файлов (File I/O)
Проект реализует сохранение состояния между запусками.
* **Input:** При старте `BankSystem` считывает `users.txt`.
* **Output:** При регистрации или изменении баланса данные перезаписываются в файл в формате `User;Pin;Balance`.

## Как запустить
1. Склонируйте репозиторий.
2. Убедитесь, что установлен JavaFX SDK.
3. Запустите файл `RealATM.java`.
